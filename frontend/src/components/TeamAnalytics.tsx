import { Typography } from "antd";
import React from "react";

const TeamAnalytics = () => {
  return (
    <div style={{ minWidth: 400 }}>
      <Typography.Title level={3} style={{ marginBottom: 24 }}>
        Best performing recruiting team
      </Typography.Title>
    </div>
  );
};

export default TeamAnalytics;
